# Rust build output
/target
**/*.rs.bk

# graphify knowledge-graph output (regenerable via `graphify update .`)
/graphify-out

# Local cargo config overrides (this repo has none; user-level config is elsewhere)
/.cargo

# Editors
.idea/
.vscode/
*.swp
*.swo

# OS
Thumbs.db
.DS_Store

# Python / misc tooling
__pycache__/
*.pyc

# Corpus PDFs — fetched via `xtask corpus fetch`, never committed (SL-0.CORP.01)
/corpus/pdfs/

tmp_*

# cargo-fuzz output (SL-0.SEC.02)
/fuzz/target/
/fuzz/corpus/
/fuzz/artifacts/
