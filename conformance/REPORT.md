# Selis conformance ladder

Every area starts at `None` and climbs as the corpus proves it. 
The published claim is never ahead of the measured level.

| Area | Level | Notes |
|---|---|---|
| COS object model | **Parse** | Parse: SL-1.COS.* DoDs; CONF.01 3,836-file parse coverage; CONF.05 zero unexplained refusals; round-trip proptests + cos_lex/cos_parse fuzz |
| Cross-reference tables and streams | **Parse** | Parse: SL-1.COS.* (xref + recovery); CONF.05 typed-refusal adjudication; incremental-save prefix proptests |
| Stream filters | **Parse** | Parse: SL-1.FILT.* + SL-2.FILT.01/02; encode-decode-identity proptests + filter_chain/jpx_stream fuzz + filter-jpx corpus; CONF.04 JPX/JBIG2 decode suspects tracked. SL-1.FILT.08 JPX decode now runs under the sandboxed wasm host; the Render rung still needs the per-filter oracle tolerance legs (Ghostscript comparison unmeasured) |
| Encryption and permissions | **Parse** | Parse: SL-1.ENCRYPT.* DoDs; engine encrypt_write_roundtrip; RC4/AESV2 read over the corpus |
| Document model (catalog, pages, outlines) | **Parse** | Parse: SL-1.DOC.* DoDs; CONF.05 MediaBox fallback renders; /Rotate page geometry closed by SL-2.RAST.12 |
| Rendering (content, raster) | **Parse** | Parse: Bar A pass (97.2% <=25% @150, n=3,774 vs MuPDF, re-measured on the post-SL-3.TEXT.11 tree; @72 96.7% n=3,778, @300 97.5% n=3,769) and Bar B passes on the MuPDF leg (<=2% 80.9 vs best pair 82.3, 1.4pp inside the 5pp allowance; <=5% 86.9, <=10% 92.6) after SL-2.RAST.14 + SL-3.TEXT.10; size_skew closed by SL-2.RAST.12 (17 -> 0), blank_selis 25 -> 2 (@150, both the /Redact-appearance /6.3.3-t01-fail-b typed deviations, SL-2.RAST.13; issue14497 still trips at 72 DPI), gross diff>=25 105 files @150 tracked by SL-2.CONF.04 (ghent 38 of them; the Prepress class stays at 60.0% <=25% vs the pair band 73.7-77.9). Render rung still unmeasured: the gate needs two *independent* oracles and the CI legs vs PDFium/pdf.js still measure 0 comparable - the bind/pin/digest causes are fixed (SL-3.CONF.02, run-34820871908 digests) and SL-3.CONF.06 now fails a 0-comparable leg in code, but the last scheduled run (34946893403, 2026-09-15 on `03ece8ba`) predates those fixes, so no post-fix pinned-container run exists yet; the next `0 3 */2 * *` window after this merge supplies the first credible legs. Any CI cell quoted from before 2026-09-14 is unconfirmed from retained artifacts. |
| Text and fonts | **Parse** | Parse: the text/font model builds and round-trips; extraction measured across the corpus vs local mutool 1.23.0 (970 >=0.99, 979 >=0.98 of 1,747 comparable = 56.04%, ge95 998, ge75 1131, median similarity 1.000, mean 0.761; 3,862 files on the post-SL-3.TEXT.11 tree - the two /bugfix_text11 fixtures add one match and one diff>=25, and everything else reproduces the 03ece8ba pass within one file). Render withheld: the text-bearing slice fails the calibrated bars on its own population (page-1 >=50 chars, n=634, @150 vs MuPDF: 89.9% <=25%, 37.9% <=2%, p50 4.08 - the 1-2% pixel band lives here), and the two-independent-oracle CI legs are unmeasured (no post-bind-fix scheduled render-conf exists; SL-3.CONF.06 now fails a 0-comparable leg in code, so the next `0 3 */2 * *` window after this merge is the first credible leg). Extract withheld (the G3 rung): 56.04% >=0.98 against the >=98%-on-95% wording, which SL-0.ORACLE.04 shows is *under* the oracle-vs-oracle ceiling (pdfium<->pdfjs 79.0%, mutool pairs ~72.7%) - recalibration owed first (SL-3.CONF.03). Open cohorts filed at CONF.02: 540 deep-tail (SL-3.CONF.03: flat 191, verapdf 196, govdocs1 90, ghent 61, synthetic 2), 62 silent annotation-AP empties (SL-3.TEXT.14), the 27-file verapdf per-glyph word-split family (13 at 12-vs-11 chars sim 0.0 + 14 short-text mid-band sim 0.043-0.58; TEXT.09's 8, grown by the re-anchor, stable across the TEXT.11 merge), the 15 rtl-flagged and 3 truncated files in the tail, the >5%-divergence watchdog (SL-3.TEXT.15), and the paint-time-Tm residual SL-3.TEXT.12 that keeps extraction honest at Parse, not Render. |
| Annotations | **None** | None at G2 (target Identify): no detection gate run; /AP appearances render since SL-2.RAST.13 (blank_selis 25 -> 2, both typed /Redact deviations); extracting text *from* those appearances is SL-3.TEXT.14 (filed by SL-3.CONF.02: 62 silent empties whose page streams are genuinely empty) |
| AcroForm | **None** | None at G2 (target Identify): no AcroForm gate run |
| Editing and incremental save | **None** | None: incremental-save machinery exists but the 20-CONFORMANCE-PROGRAM.md §3 Edit gate suite is unmeasured (Phase 5) |
| Redaction | **None** | None (Phase 5; no crate yet) |
| Digital signatures | **None** | None (Phase 5; no crate yet) |
| PDF/A | **None** | None (Phase 9 target Author; structural rules evaluable via the DOC.09 registry) |
| PDF/UA | **None** | None (Phase 9 target Author; structural rules evaluable via the DOC.09 registry) |
