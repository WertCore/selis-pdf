# Conformance areas — SL-0.OPS.02.
#
# Machine-readable twin of 20-CONFORMANCE-PROGRAM.md. Every area starts at
# level 0 (None). `xtask conformance report` renders the current state as the
# project's real status page; CI publishes it.
#
# Ladder levels (20-CONFORMANCE-PROGRAM.md):
#   0 None       — nothing implemented, nothing claimed.
#   1 Identify   — the engine recognises the construct.
#   2 Parse      — the engine parses it without loss.
#   3 Render     — it renders correctly.
#   4 Extract    — text/structure can be extracted.
#   5 Edit       — it can be modified and saved back.
#   6 Author     — the engine can author new documents using it.
#
# Promotion record (SL-3.CONF.02, 2026-09-14): second ladder update, on the post-
# merge tree (3,860-file regenerated corpus incl. the SL-3.TEXT.10 BT fix). `text`
# moves None -> Parse (Unicode extraction measured across the corpus; Render and
# Extract both withheld — see the row). `render` stays Parse with the bar
# re-measured: it now clears Bar A and Bar B on the MuPDF leg, and only the
# two-independent-oracle CI legs keep it down. Tolerances were read against
# SL-0.ORACLE.04's noise floor, not against the red build.

# Promotion record (SL-2.CONF.02, 2026-09-11): first ladder update. Levels are
# measurements against the SL-2.CONF.03 calibrated bar (Bar A: >=95% of
# comparable pages <=25% differing pixels @150 DPI; Bar B: selis's <=2%/<=5%/
# <=10% band fractions within 5pp of the best oracle pair). No area meets the
# full bar — Bar B fails at <=2% (76.2 vs best pair 82.3, 1.1pp past the 5pp
# allowance; SL-2.RAST.14) — so nothing promotes to Render. Areas with
# measured Parse evidence sit at Parse; everything unmeasured stays at None,
# including the G2 Identify targets for annotations/AcroForm (ADR-P0010).

[[area]]
id = "cos"
name = "COS object model"
level = 2
notes = "Parse: SL-1.COS.* DoDs; CONF.01 3,836-file parse coverage; CONF.05 zero unexplained refusals; round-trip proptests + cos_lex/cos_parse fuzz"

[[area]]
id = "xref"
name = "Cross-reference tables and streams"
level = 2
notes = "Parse: SL-1.COS.* (xref + recovery); CONF.05 typed-refusal adjudication; incremental-save prefix proptests"

[[area]]
id = "filters"
name = "Stream filters"
level = 2
notes = "Parse: SL-1.FILT.* + SL-2.FILT.01/02; encode-decode-identity proptests + filter_chain/jpx_stream fuzz + filter-jpx corpus; CONF.04 JPX/JBIG2 decode suspects tracked. SL-1.FILT.08 JPX decode now runs under the sandboxed wasm host; the Render rung still needs the per-filter oracle tolerance legs (Ghostscript comparison unmeasured)"

[[area]]
id = "encryption"
name = "Encryption and permissions"
level = 2
notes = "Parse: SL-1.ENCRYPT.* DoDs; engine encrypt_write_roundtrip; RC4/AESV2 read over the corpus"

[[area]]
id = "document"
name = "Document model (catalog, pages, outlines)"
level = 2
notes = "Parse: SL-1.DOC.* DoDs; CONF.05 MediaBox fallback renders; /Rotate page geometry closed by SL-2.RAST.12"

[[area]]
id = "render"
name = "Rendering (content, raster)"
level = 2
notes = "Parse: Bar A pass (97.2% <=25% @150, n=3,773 vs MuPDF, post-merge re-run) and Bar B now passes on the MuPDF leg (<=2% 80.9 vs best pair 82.3, 1.4pp inside the 5pp allowance; <=5% 86.8, <=10% 92.6) after SL-2.RAST.14 + the SL-3.TEXT.10 BT fix; size_skew closed by SL-2.RAST.12 (17 -> 0), blank_selis 25 -> 2 (typed /Redact-appearance deviations, SL-2.RAST.13), gross diff>=25 106 files tracked by SL-2.CONF.04. Render rung still unmeasured: the gate needs two independent oracles, and the CI legs against PDFium/pdf.js failed on stale digests (re-pinned in this wave) - confirming run pending"

[[area]]
id = "text"
name = "Text and fonts"
level = 2
notes = "Parse: text/font models build and decode; extraction measured across the 3,860-file corpus vs pinned mutool 1.23.0 (969 >=0.99, 978 >=0.98 of 1,745 comparable, median similarity 1.000, mean 0.761). Render withheld: the text-bearing slice itself (>=50 chars, n=635) renders 89.8% <=25% @150 vs MuPDF, below Bar A, and two-independent-oracle evidence awaits the CI re-run (SL-3.CONF.03/05). Extract withheld: 56.05% >=0.98 vs the >=98%-of-corpus G3 wording, which SL-0.ORACLE.04 shows sits under the oracle-vs-oracle ceiling (79.0% pdfium<->pdfjs, ~72.7% mutool pairs) - recalibration owed (SL-3.CONF.03) with the open cohorts: 539 deep-tail (SL-3.CONF.03), 62 silent annotation-AP empties (SL-3.TEXT.12), 13-file MuPDF word-split cohort awaiting OracleBug/OurBug verdicts (SL-3.CONF.04)"

[[area]]
id = "annot"
name = "Annotations"
level = 0
notes = "None at G2 (target Identify): no detection gate run; /AP appearances render since SL-2.RAST.13 (blank_selis 25 -> 2, both typed /Redact deviations); extracting text from those appearances is SL-3.TEXT.12 (filed by SL-3.CONF.02: 62 silent empties whose page streams are genuinely empty)"

[[area]]
id = "forms"
name = "AcroForm"
level = 0
notes = "None at G2 (target Identify): no AcroForm gate run"

[[area]]
id = "edit"
name = "Editing and incremental save"
level = 0
notes = "None: incremental-save machinery exists but the 20-CONFORMANCE-PROGRAM.md §3 Edit gate suite is unmeasured (Phase 5)"

[[area]]
id = "redact"
name = "Redaction"
level = 0
notes = "None (Phase 5; no crate yet)"

[[area]]
id = "sign"
name = "Digital signatures"
level = 0
notes = "None (Phase 5; no crate yet)"

[[area]]
id = "pdfa"
name = "PDF/A"
level = 0
notes = "None (Phase 9 target Author; structural rules evaluable via the DOC.09 registry)"

[[area]]
id = "pdfua"
name = "PDF/UA"
level = 0
notes = "None (Phase 9 target Author; structural rules evaluable via the DOC.09 registry)"
